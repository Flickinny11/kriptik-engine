{
  "name": "kriptik",
  "version": "0.1.0",
  "private": true,
  "description": "KripTik — autonomous AI-powered app-building platform powered by the Cortex Engine",
  "type": "module",
  "scripts": {
    "build": "tsc --build",
    "typecheck": "tsc --build",
    "test": "pnpm -r run test",
    "clean": "pnpm -r exec rm -rf dist"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0"
  }
}
