#!/usr/bin/env sh

# Warn if frozen auth files are modified (social login spec)
bash scripts/check-auth-freeze.sh

# Block commits with spec violations (wrong models/tech)
bash scripts/check-spec-violations.sh
